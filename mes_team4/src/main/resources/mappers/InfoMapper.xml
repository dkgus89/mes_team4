<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 파일이름 대신 namespace 이름을 불러서 사용 -->

<mapper namespace="com.itwillbs.mappers.InfoMapper">
	
	<!-- ?대신 memberDTO의 멤버변수명 사용 #{id}-->
<!-- 	<insert id="insertMember"> -->
<!-- 		insert into members(id,pass,name,date)  -->
<!-- 		values(#{id},#{pass},#{name},#{date}) -->
<!-- 	</insert> -->

	<insert id="insertLine">
    	insert into line(line_cd, line_name, line_process, line_sequence, line_status, remarks)
    	values(#{line_cd},#{line_name},#{line_process},#{line_sequence},#{line_status},#{remarks})
    </insert>

	<select id="getLineList" resultType="com.itwillbs.domain.LineDTO">
    	select * from line 
    </select>

	<select id="getLine" resultType="com.itwillbs.domain.LineDTO">
        select * from line
        where line_cd = #{line_cd}
    </select>
    
    <update id="updateLine">
    	update line
    	set line_name = #{line_name}, line_process = #{line_process}, line_sequence = #{line_sequence}, line_status = #{line_status}, remarks = #{remarks}
    	where line_cd = #{line_cd}
    </update>
	
<!-- 	<select id="userCheck" resultType="com.itwillbs.domain.MemberDTO"> -->
<!-- 		select * from members  -->
<!-- 		where id = #{id} and pass = #{pass}  -->
<!-- 	</select> -->

<!-- 	<select id="selectBlog" parameterType="int" resultType="Blog">
        select * from Blog where id = #{id}
    </select>
    
    <insert id="">
    </insert>
    
    <update id="">
    </update>
    
    <delete id="">
    </delete> -->
    
</mapper>



